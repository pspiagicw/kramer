(fn is_even (n)
  (if (== n 0)
    true
    (is_odd (- n 1))))

(fn is_odd (n)
  (if (== n 0)
    false
    (is_even (- n 1))))

(print (is_even 0))
(print (is_even 4))
(print (is_even 7))

(print (is_odd 1))
(print (is_odd 6))
(print (is_odd 9))
